# Database Seeds

SQL files with initial/seed data:

- `roles.sql` - Initial roles (ADMIN, SUPERVISOR, BECA)
- `schedule-types.sql` - Schedule types
- `selection-states.sql` - Selection process states
- `beca-states.sql` - Scholarship states
- `create-admin-user.sql` - Create default admin user
